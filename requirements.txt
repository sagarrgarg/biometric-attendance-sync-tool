requests
# Pinned exact: the code relies on PickleDB(path) auto-loading in __init__ and
# on .save(). pickledb 1.6 changed to a chained .load() (no auto-load), which
# would start with an empty db and overwrite status.json on first save. 0.9.x
# lacks .save(). 1.3.2 is the verified-compatible release.
pickledb==1.3.2
pyzk
PyQt5
